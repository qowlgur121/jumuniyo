# jumuniyo/frontend/.env.development
# .env.development 파일은 '개발(development)' 환경에서 사용할 설정들을 넣어두는 파일

# 백엔드 API 서버의 기본 주소를 설정함.
# Vite에서 환경 변수로 사용하려면 변수 이름 앞에 VITE_ 를 붙여야 함.
VITE_APP_API_BASE_URL=http://localhost:8081

# 만약 다른 환경 변수도 필요하다면 여기에 추가하면 됨.
# 예: VITE_APP_KAKAO_MAP_API_KEY=카카오맵_API_키